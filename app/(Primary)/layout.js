export const metadata = {
    title: 'Gold Star',
    description: 'Thats a lorem text for text output website.',
};
export default function MainLayout({ children }) {

    return (
        <>
            {children}
        </>
    );
}
